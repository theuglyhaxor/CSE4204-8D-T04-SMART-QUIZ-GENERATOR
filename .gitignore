# --- Secrets -----------------------------------------------------------------
.env
*.env
backend/.env
# ...but the templates must stay in the repo:
!.env.example
!backend/.env.example
!frontend/.env.example

# --- Python ------------------------------------------------------------------
__pycache__/
*.pyc
.venv/
venv/
env/

# --- Database ----------------------------------------------------------------
# The dev SQLite database is created by `manage.py migrate` — never commit it.
*.sqlite3
*.sqlite3-journal

# --- Node / frontend ---------------------------------------------------------
node_modules/
frontend/dist/
frontend/.vite/

# --- Editor / OS -------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/
